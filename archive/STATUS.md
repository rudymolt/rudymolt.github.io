# archive/ status

archived_features: 0

> **This is a definitive empty state, not an error.** Created at bootstrap. If this file says 0 and there are no dated folders alongside it, nothing has shipped yet — the empty folder is the answer, not a problem.

This file is the **only** thing in `archive/` an agent may read (besides `README.md`). It exists so the count is available without listing or opening archived contents.

## Maintenance rule

Stage 10 (ship) moves `planning/{slug}/` here as `{YYYY-MM-DD}-{slug}/` → increment the count and add a line below.

## Archived features

*(none — add one line per shipped feature: `- {YYYY-MM-DD}-{slug}`)*
